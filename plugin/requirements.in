# Runtime dependencies of the plugin's MCP server, pinned to the versions
# Locus's own runtime uses (see ../constraints.txt). Compile with
# tools/build_plugin.py; the launcher installs requirements.lock with hashes.
langgraph==1.2.2
langgraph-checkpoint==4.2.0
langgraph-checkpoint-sqlite==3.1.1
langgraph-sdk==0.3.15
mcp==2.0.0
